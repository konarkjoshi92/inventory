import 'package:flutter/material.dart';

class NewProductEntry extends StatelessWidget {
  const NewProductEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
